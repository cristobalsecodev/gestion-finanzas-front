import { SimboloDivisaPipe } from './simbolo-divisa.pipe';

describe('SimboloDivisaPipe', () => {
  it('create an instance', () => {
    const pipe = new SimboloDivisaPipe();
    expect(pipe).toBeTruthy();
  });
});
